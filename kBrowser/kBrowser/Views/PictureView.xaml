<UserControl x:Class="kBrowser.Views.PictureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:k="http://schemas.microsoft.com/kinect/2013"
             xmlns:b="clr-namespace:kBrowser.Businesses"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <k:KinectCircleButton Command="{x:Static b:ViewCommands.jumpToViewCommand}" CommandParameter="{x:Static b:ViewModels.overallViewType}" Height="100">
                ←
            </k:KinectCircleButton>
            <TextBlock Text="{Binding fileName}"/>
            <k:KinectCircleButton Height="100" Label="Preview" Command="{x:Static b:ViewCommands.loadPreviousPictureCommand}"/>
            <k:KinectCircleButton Height="100" Label="Next" Command="{x:Static b:ViewCommands.loadNextPictureCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Image Source="{Binding uri}" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="{Binding Value, ElementName=sl_scale}" ScaleY="{Binding Value, ElementName=sl_scale}"/>
                        <SkewTransform/>
                        <RotateTransform Angle="{Binding Value, ElementName=sl_rotate}"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
            <k:KinectSlider x:Name="sl_scale" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Stretch" Minimum="0" Maximum="10" Value="1"/>
            <k:KinectSlider x:Name="sl_rotate" HorizontalAlignment="Stretch" VerticalAlignment="Top" Minimum="-180" Maximum="180"/>
        </Grid>
    </Grid>
</UserControl>
